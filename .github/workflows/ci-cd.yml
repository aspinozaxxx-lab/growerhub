name: CI/CD Server

on:
  push:
    branches: [ "main" ]
    paths:
      - "server/**"
      - "static/**"
  workflow_dispatch:

jobs:
  test:
    name: Test (pytest)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: watering_db
          POSTGRES_USER: watering_user
          POSTGRES_PASSWORD: watering_pass
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U watering_user -d watering_db"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('server/requirements.txt') }}
          restore-keys: ${{ runner.os }}-pip-

      - name: Install deps
        working-directory: server
        run: |
          python -m venv .venv
          . .venv/bin/activate
          pip install -U pip
          pip install -r requirements.txt || true
          pip install pytest

      - name: Set DATABASE_URL
        run: echo "DATABASE_URL=postgresql+psycopg2://watering_user:watering_pass@localhost:5432/watering_db" >> $GITHUB_ENV

      - name: Run Alembic migrations
        working-directory: server
        run: |
          . .venv/bin/activate
          alembic upgrade head

      - name: Run pytest DEBUG
        working-directory: server
        run: |
          . .venv/bin/activate
          pytest tests/test_auth_users.py::test_diag_authenticate_local_user_steps -q
          
      - name: Run pytest
        working-directory: server
        run: |
          . .venv/bin/activate
          pytest -q --maxfail=1 --disable-warnings --junitxml=pytest-report.xml

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-report
          path: server/pytest-report.xml

  deploy:
    name: Deploy to production
    runs-on: ubuntu-latest
    needs: [ test ]
    if: ${{ needs.test.result == 'success' && github.ref == 'refs/heads/main' }}
    environment:
      name: production
      url: https://growerhub.ru/
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          install -m 700 -d ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -p 6733 growerhub.ru > ~/.ssh/known_hosts
          chmod 644 ~/.ssh/known_hosts

      - name: SSH smoke test
        run: |
          ssh -p 6733 -o StrictHostKeyChecking=yes watering-admin@growerhub.ru "echo 'SSH OK'"

      - name: Ensure target dir exists
        run: |
          ssh -p 6733 -o StrictHostKeyChecking=yes watering-admin@growerhub.ru \
            "mkdir -p /home/watering-admin/growerhub && chmod 755 /home/watering-admin/growerhub"

      - name: Sync project to server
        run: |
          rsync -azv --delete \
            --exclude ".git/" --exclude ".venv/" --exclude "__pycache__/" --exclude "*.pyc" \
            --omit-dir-times --no-perms --no-owner --no-group \
            -e "ssh -p 6733 -o StrictHostKeyChecking=yes" \
            ./ watering-admin@growerhub.ru:/home/watering-admin/growerhub/

      - name: Remote install dependencies
        run: |
          ssh -p 6733 watering-admin@growerhub.ru <<'EOF'
          set -e
          cd ~/growerhub/server
          python3 -m venv .venv
          . .venv/bin/activate
          pip install -U pip
          pip install -r requirements.txt
          EOF

      - name: Remote Alembic upgrade
        run: |
          ssh -p 6733 watering-admin@growerhub.ru <<'EOF'
          set -e
          cd ~/growerhub/server
          . .venv/bin/activate
          DATABASE_URL=$(sudo systemctl show --property=Environment growerhub.service | tr ' ' '\n' | sed -n 's/^DATABASE_URL=//p')
          if [ -z "$DATABASE_URL" ]; then
            echo "DATABASE_URL is not set in growerhub.service" >&2
            exit 1
          fi
          export DATABASE_URL
          alembic upgrade head
          EOF

      - name: Remote restart service
        run: |
          ssh -p 6733 watering-admin@growerhub.ru <<'EOF'
          set -e
          cd ~/growerhub/server
          sudo systemctl daemon-reload || true
          sudo systemctl restart growerhub.service
          systemctl status growerhub.service --no-pager || true
          EOF
